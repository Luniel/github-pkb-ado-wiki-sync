# Compact Examples

## Generic login scenarios
- Example finding: `Then user is logged in` without grounded credential/identity conditions.
- Candidate follow-up: current-spec correction for missing Given state; defer lockout-policy branch for stakeholder scope decision.

## Browser page title Scenario Templates
- Example finding: one template row expects a different outcome title family than other rows.
- Candidate follow-up: split into separate scenario families; classify conflicting row as create now or reject.

## Repo-backed Browser Page Titles / Application Identity / Domain Language set
- Example finding: title wording rule appears in support notes while behavior spec is silent.
- Candidate follow-up: classify as upstream dependency concern vs support artifact clarification; preserve open research question if ownership is unclear.
