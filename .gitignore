# Local config
sync.config.json

# PowerShell / editor noise
*.log
*.tmp
.DS_Store
Thumbs.db

# Local-only folders
history/
local-only/

# Local runtime sync configuration/state (examples remain tracked at repo root)
configs/*.sync.json
configs/*.sync.state.json
